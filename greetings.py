#Create a new Python script, greetings.py to accept user’s input.
#The input() method returns inputs read from the keyboard. The values are always of thestring type. Prompts user for name and store into a variable. Then greet the user with his name.
#The script runs through and prompts the user to enter their name which is then stored into the variable 'msg'. The user is greeted with their name based on the input in the prompt.

msg = input("Please enter your name: ")
print("Hello, " + msg + "." + " Welcome to this program!")